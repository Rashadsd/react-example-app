// Created index.js file
// Installed the development environment on React using the command in the terminal: npx create-react-app my-app
// I went to the src folder and made a launch using the command  npm start
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  
    <App/>

);


